You must provide the filepath to a JSON file containing required information to connect into a MySQL database.

JSON format example below:

```javascript
{
	"host": "ip",
	"username": "user",
	"password": "pass"
}
```
